<launch>


  <include file="$(find gazebo_ros)/launch/empty_world.launch">
      <!--
      <arg name="world_name" value="$(find exmpl_models)/terrain_world/terrain.world"/>
      -->
      <arg name="world_name" value="$(find exmpl_models)/terrain/training.world"/>
  </include>

<!--
  <param name="robot_description" command="$(find xacro)/xacro.py '$(find sentry_urdf)/urdf/sentry_standalone.urdf.xacro'" />

  <node name="spawn_robot" pkg="gazebo_ros" type="spawn_model"
      args="-param robot_description
            -urdf
            -z 0.7
            -model robot_description"
      respawn="false" output="screen" />
-->

</launch>
